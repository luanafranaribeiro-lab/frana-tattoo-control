// Organização visual do inventário por função no processo da tattoo.
const STOCK_GROUPS=[
 {key:'tattoo',label:'🖊 Tattoo',types:['Cartucho','Tinta','Batoque']},
 {key:'prep',label:'🧴 Preparo & aplicação',types:['Sabonete','Transfer','Vaselina','Gilete']},
 {key:'hygiene',label:'🧤 Proteção & higiene',types:['Luvas','Máscara','Plástico filme','Fita crepe','Bandagem']},
 {key:'paper',label:'📄 Papéis & impressão',types:['Papel']},
 {key:'after',label:'🩹 Pós-tattoo',types:['Vitalderm','Cartão pós tattoo','Potinho','Pomada']},
 {key:'support',label:'🥄 Descartáveis & apoio',types:['Palito']},
 {key:'other',label:'📦 Outros',types:['Outro']}
];
function stockGroupOf(s){let t=typeof stockTypeOf==='function'?stockTypeOf(s):(s.type||s.customType||s.cat||'Outro');let g=STOCK_GROUPS.find(x=>x.key!=='other'&&x.types.includes(t));return g||STOCK_GROUPS.find(x=>x.key==='other')}
function stockGroupItems(key){return db.stock.filter(s=>(+s.qty||0)>0&&stockGroupOf(s).key===key)}
function stockGroupCard(g){let items=stockGroupItems(g.key);if(!items.length)return'';return`<button class="card row stock-group-card" style="width:100%;text-align:left" onclick="stockGroupView('${g.key}')"><div><b>${g.label}</b><div class="tiny">${items.length} ${items.length===1?'item':'itens'}</div></div><b>›</b></button>`}
function stockCompactRow(s){if(s?.isMimoReady)mimoRefreshCost();return`<div class="card stock-search-item" data-search="${esc(`${s.name} ${stockTypeOf(s)} ${s.brand||''} ${s.model||''} ${s.num||''} ${s.config||''}`.toLowerCase())}" onclick="${s.isMimoReady?'mimoView()':`stockSheet(${s.id})`}"><div class="row"><div style="min-width:0"><b>${esc(s.name)}</b><div class="tiny">${esc(stockTypeOf(s))} · ${money(s.cost)}/${esc(s.unit||'un')}</div></div><b class="${(+s.min||0)>0&&(+s.qty||0)<=+s.min?'red':''}">${fmt(s.qty)} ${esc(s.unit||'un')}</b></div></div>`}
function lowStockRow(s){return`<div class="card" onclick="${s.isMimoReady?'mimoView()':`stockSheet(${s.id})`}"><div class="row"><div style="min-width:0"><b>${esc(s.name)}</b><div class="tiny">Mínimo: ${fmt(s.min)} ${esc(s.unit||'un')}</div></div><b class="red">${fmt(s.qty)} ${esc(s.unit||'un')}</b></div></div>`}
function filterStockSearch(input,target){let q=(input.value||'').trim().toLowerCase(),box=document.querySelector(target);if(!box)return;box.querySelectorAll('.stock-search-item').forEach(el=>el.style.display=!q||el.dataset.search.includes(q)?'':'none')}
function stockSearchBox(id,target,placeholder='Pesquisar material'){return`<div class="row" style="gap:8px;align-items:center"><span aria-hidden="true">🔎</span><input id="${id}" type="search" placeholder="${placeholder}" oninput="filterStockSearch(this,'${target}')" style="margin:0;flex:1"></div>`}
function stockGroupView(key){let g=STOCK_GROUPS.find(x=>x.key===key),items=stockGroupItems(key);form(g?.label||'Estoque',stockSearchBox('groupStockSearch','#groupStockResults')+`<div id="groupStockResults">${items.map(stockCompactRow).join('')||empty('Nenhum item nesta categoria.')}</div>`,closeM,'Fechar')}
function stockSearchView(){mimoRefreshCost();form('Pesquisar inventário',stockSearchBox('allStockSearch','#allStockResults')+`<div id="allStockResults">${db.stock.filter(x=>(+x.qty||0)>0).map(stockCompactRow).join('')||empty('Nenhum item em estoque.')}</div>`,closeM,'Fechar');setTimeout(()=>$('#allStockSearch')?.focus(),50)}
function mimoReadyCard(){let m=mimoRefreshCost();return `<button class="card full-card" onclick="mimoView()"><div class="row"><div><b>🎁 Mimo pronto</b><div class="tiny">Produzir ou editar composição · ${money(m.cost)}/un</div></div><b>${fmt(m.qty)} un</b></div></button>`}
function stock(){let low=db.stock.filter(x=>(+x.min||0)>0&&(+x.qty||0)<=+x.min);return`<div class="brand">Estoque</div><button class="btn full" onclick="stockEntry()">+ Adicionar compra / material</button><div class="grid stock-actions"><button class="btn light" onclick="stockManager()">Editar estoque</button><button class="btn light" onclick="kitsView()">Kit Flash Evento</button><button class="btn light" onclick="purchasesView()">Itens comprados</button><button class="btn light" onclick="shoppingView()">Lista de compras (${shopping().length})</button></div>${mimoReadyCard()}${low.length?`<h2>⚠️ Estoque baixo</h2>${low.map(lowStockRow).join('')}`:''}<div class="section-head"><h2>Inventário</h2><button class="btn light" onclick="stockSearchView()" aria-label="Pesquisar inventário">🔎</button></div>${STOCK_GROUPS.map(stockGroupCard).join('')||'<div class="card sub">Nenhum item em estoque.</div>'}`}
// Pesquisa também dentro de Editar estoque, preservando a edição existente.
const _stockManagerSearchBase=stockManager;
stockManager=function(){_stockManagerSearchBase();let m=$('#modal>div');if(!m)return;let cards=[...m.querySelectorAll('.card')];cards.forEach(c=>{c.classList.add('stock-search-item');c.dataset.search=(c.textContent||'').toLowerCase()});let h=m.querySelector('h2');if(h)h.insertAdjacentHTML('afterend',stockSearchBox('editStockSearch','#modal','Pesquisar no estoque'))}
